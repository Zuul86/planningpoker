{
  "name": "notify-all-at-table",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
 "scripts": {
    "build": "esbuild index.ts --bundle --minify --sourcemap --platform=node --target=es2020 --outfile=dist/index.js",
    "publish": "cd dist && aws lambda update-function-code --function-name notify-all-at-table --zip-file fileb://index.zip --publish"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "@types/aws-lambda": "^8.10.109",
    "esbuild": "^0.16.12"
  },
  "dependencies": {
    "@aws-sdk/client-apigatewaymanagementapi": "^3.238.0",
    "@aws-sdk/client-dynamodb": "^3.238.0",
    "@aws-sdk/util-dynamodb": "^3.238.0"
  }
}
