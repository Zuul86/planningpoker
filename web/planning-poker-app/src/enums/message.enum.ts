export enum Message {
  JoinTable = 'join-table',
  VoteEffort = 'vote-effort',
  RevealEfforts = 'reveal-efforts',
  ResetVote = 'reset-vote',
  UserJoined = 'user-joined',
  UserVoted = 'user-voted'
}